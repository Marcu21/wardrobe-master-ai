import 'package:flutter/material.dart';

// Background
const Color kBgColor = Color(0xFFF4F3F0);

// Brand purple scale
const Color kPrimary      = Color(0xFF4F46E5);
const Color kPrimaryLight = Color(0xFFEEEDF8);
const Color kPrimaryMid   = Color(0xFF8B85D4);

// Glass morphism surfaces
const Color kGlass       = Color(0xCCFFFFFF); // white 80 %
const Color kGlassBorder = Color(0xE5FFFFFF); // white 90 %

// Semantic status
const Color kSuccess = Color(0xFF22C55E); // positive / score good
const Color kWarning = Color(0xFFF59E0B); // attention / score moderate
const Color kDanger  = Color(0xFFDC2626); // destructive actions / delete
const Color kError   = Color(0xFFEF4444); // error feedback / snackbar
const Color kInfo    = Color(0xFF6366F1); // informational snackbar

// CTA button gradient (primary action buttons)
const Color kCtaStart = Color(0xFF5B52F0);
const Color kCtaEnd   = Color(0xFF3730C8);

// Surface / text
const Color kSurfaceDark = Color(0xFF18181B); // dark card / snackbar bg
const Color kTextDark    = Color(0xFF1A1A1A); // near-black text on light cards

// Page background glows — indigo variant (home, wardrobe, laundry, outfit, add-clothing, lookbook)
const Color kGlowPrimary  = Color(0x384F46E5);
const Color kGlowPrimary2 = Color(0x206352D2);

// Page background glows — blue variant (trips, packing, item-selection)
const Color kGlowBlue  = Color(0x3840C4FF);
const Color kGlowBlue2 = Color(0x1E1565C0);

// Blue accent (trips, calendar, packing)
const Color kBlueAccent = Color(0xFF1565C0);
const Color kBlueBright = Color(0xFF40C4FF);
